import { WorkbenchLoadingState } from "@/components/workbench";

export default function Loading() {
  return (
    <WorkbenchLoadingState
      title="Loading Company onboarding"
      description="Preparing the company setup workflow, statutory facts, registered office fields and officer evidence."
    />
  );
}
