# Nightly Completion Log — Irish Statutory Accounts Platform

Branch: `nightly/completion-2026-06-20`
Started: 2026-06-20

## Baseline (step 1)
- **Backend**: `dotnet test Accounts.slnx -c Release -p:ArtifactsPath=$env:TEMP/accts-art` →
  **468 passed, 2 skipped, 470 total** (exit 0). The 2 skips are the Postgres-only audit
  integration tests (expected on the InMemory provider — see BL-15).
- **Frontend**: `npm run build` (exit 0) and `npm run lint` (exit 0) — both clean.

Any later regression below this line is attributable to the session's changes.

---

## Items

<!-- One line per item: BL-id — what changed — test added — result -->
- **BL-04** ✅ — `TaxComputationService.ComputeAsync` now separates the Case I trading stream from the s.21A non-trading stream; a trading loss is carried forward (s.396(1)) and no longer shelters passive income from the 25% charge (s.396A loss relief is NOT auto-applied — Human decision #3, conservative default). Test `TaxComputation_TradingLossDoesNotShelterNonTradingIncomeFrom25Percent` (trading loss €10k + rental €4k → CT@25% = €1,000, full €10k loss carried forward). Full suite **469 pass / 2 skip** (exit 0). Commit `d0e2f16`.
  - FLAG (Human decision #3): loss-relief election (s.396A/value-basis) is not implemented; passive income taxed at 25% by default. Expose the election later.
- **BL-01** ✅ — Took the **conservative balancing-adjustments path** (not the single-source refactor). `AdjustmentService.GenerateAutoAdjustmentsAsync` now posts double-entry adjustments for trade debtors (→ turnover) and trade creditors (→ expense), mirroring the existing accrual/prepayment/stock pattern, so the reserves side moves in step with the entity-table net-assets side. Test `BalanceSheet_MixedCashAccrualScenario_BalancesWithZeroUnexplainedDifference` (full mixed set → `Balances == true`, diff == 0, net assets == capital == €7,200). Full suite **470 pass / 2 skip** (exit 0). Commit `de9e552`.
  - FLAG (Human decision #6): single-source-of-truth vs balancing-adjustments is an architectural fork. Landed the safe balancing path. Notes/limits: (a) Trade debtors/creditors only — **Other** debtors/creditors have an ambiguous contra and still need a manual adjustment; (b) recognition is closing-balance-based (consistent with existing accrual/prepayment/stock), correct for first-year/self-contained periods — multi-year movement-basis is BL-20; (c) a loan only balances if its drawdown cash is present as a bank transaction (the engine reads cash from transactions, so it cannot synthesise the missing cash leg).
- **BL-30** ✅ — Added `Docs/operations/api-key-rotation.md` (zero-downtime rotation procedure for the single service API key, with rollback). Test `ApiKeyRotationRunbook_DocumentsHashAndZeroDowntimeRotation`. Commit `f5704ee`.
  - FLAG (Human decision #8): the key is single/unscoped (Admin-level); per-company-scoped keys with independent rotation are a larger future investment.
- **Final pass** ✅ — Backend **505 pass / 2 skip (507 total)**; frontend `lint` + `tsc --noEmit` + `build` + all verify scripts (incl. new `test:api-client`) clean; `dotnet ef migrations has-pending-model-changes` clean; CLAUDE.md stats reconciled (tests 463→507, entities 43→44, migrations 18→19, DbSets 42→43).
- **BL-32** ✅ — Figure-level tests: prepayment increases profit + shows as current asset; PAYE/RCT balances are within-year creditors; capex coded to an asset account does not reduce profit (capital vs revenue); prior-period profit rolls into opening retained earnings. Commit `b809449`.
- **BL-10** ✅ (guard test landed; EF filter flagged) — Behavioural `TenantIsolation_CompanyAndPeriodAccessIsScopedToCallersTenant` proves `CanAccessCompany*` filter to the caller's tenant at the data-access query layer (cross-tenant id invisible → 404). Commit pending in BL-10/27 batch.
  - FLAG (Human decision #8): a DB-level **EF global query filter** (`HasQueryFilter` on `TenantId`) as a hard backstop is the bigger investment, deferred — it needs an ambient tenant accessor wired into the DbContext. The query-layer scoping + extensive source-level guards are the current defence.
- **BL-27** ✅ — Exact-date period-membership boundary tests for fixed assets (acquired/disposed on vs after year-end) and share capital (issued/cancelled on vs after year-end). Commit pending in BL-10/27 batch.
- **BL-21** ✅ — Reducing-balance depreciation now writes off the remaining book value in the final year of useful life (was asymptotic, never reaching 0). Test `Depreciation_ReducingBalanceFullyWritesDownByEndOfUsefulLife` (8,000 / 3yr → closing NBV 0, total charge 8,000). Commit `67e1ff6`.
- **BL-22** ✅ — First-year SOCIE opening share capital now uses capital subscribed at incorporation (was hard-coded 0, contradicting its own comment), so the SOCIE agrees with the balance sheet. Test `EquityChanges_FirstYearShowsIncorporationCapitalAsOpeningNotIssuedInYear`. Commit `c4c4e91`.
- **BL-31** ✅ — Backend `SecurityHeadersMiddleware` now emits a strict CSP on `/api` and HSTS over HTTPS (extracted `ApplyTo` for testing). Rate-limit XFF was already config-gated (`TrustForwardedFor` default false, production-safety-enforced); added a test locking the safe default in. Tests `SecurityHeaders_EmitCspOnApiAndHstsOverHttps`, `RateLimitClientKey_IgnoresForwardedForUnlessExplicitlyTrusted`. Commit `ea3bd50`.
- **BL-29** ℹ️ mostly already satisfied — the deprecated `mark-generated` route already returns **410 Gone** (`FilingWorkflowEndpoints.cs:101`). The `reports` table is still referenced by `SeedData` (dev-only seeding), so it is not safe to drop tonight without a migration + seed change — left in place. CLAUDE.md route/stat drift is reconciled in the final pass. No separate commit.
- **BL-14** ✅ — Tests for AIB/BOI/Revolut/Stripe header auto-detection (`ImportService_DetectsBankFormatFromHeader`) + end-to-end Revolut import with column-mapping + duplicate detection (`ImportService_AutoDetectsRevolutAndParsesColumnsPerMapping`). Commit `7929cea`.
  - FLAG: the AIB/BOI/Stripe **column index** mappings are still unvalidated against real bank exports (the coded AIB `DateColumn=0` looks wrong — real AIB col 0 is the account number). Detection-by-keyword is tested; the index maps need a real sample to confirm/fix. Left as-is to avoid guessing.
- **BL-15** ✅ (CI-only verification) — CI `Backend` job now runs a `postgres:16.4` service and sets `ACCOUNTS_POSTGRES_TEST_CONNECTION`, so the 2 `[PostgresFact]` audit-durability tests execute instead of skipping. CLAUDE.md reconciled. NOTE: not locally verifiable (Linux CI + Postgres) — CI is the source of truth. Commit `7929cea`.
- **BL-16** ✅ — Boundary test `DirectorLoanCompliance_TenPercentNetAssetsThresholdBoundary` (below/at/above 10% of net assets; SAP + s.239 warning). Commit `7929cea`.
  - FLAG (Human decision #4): code constant/warning say **s.239**; docs say **s.236**. Test asserts the coded s.239 behaviour; the section discrepancy needs legal confirmation.
- **BL-17** ✅ — Tests the default chart of accounts (`CategoryService_SeedsDefaultIrishChartOfAccounts`, key codes/types + idempotent re-seed) and confidence-scored auto-categorisation (`CategoryService_AutoCategorisesByRuleThenFuzzyNameWithConfidence`: rule 0.85, fuzzy 0.5, none 0). Commit `7929cea`.
- **BL-05** ⚠️ PARTIAL (client + backend done & tested; UI deferred) — Added the typed api.ts CRUD client for loans (+ balance snapshots), director loans and share capital, plus update helpers for year-end rows. Refactored `ApiError` off TS parameter properties so api.ts loads under Node strip-only stripping; new `verify-api-client` script (npm `test:api-client`) asserts every client function's method+URL. Backend was already fully wired (all PUTs exist). Commit `6ecbaef`.
  - REMAINING (flagged): the year-end **page UI** sections (add/edit/delete forms for loans, director loans, share capital) are not yet wired into `year-end/page.tsx`. Deferred because (a) the page is a 1,900-line Next.js 16 client component (the repo's `frontend/AGENTS.md` warns Next 16 has breaking changes beyond training data), (b) there is no frontend component-test framework (BL-19) so only `build`/`lint` can verify UI, and (c) director-loan entry needs an officer picker. The api.ts client + backend are ready for this wiring. Data can already be entered via the API today.
- **BL-25** ✅ — Added the missing Dividend update (PUT) endpoint (`UpdateDividendEndpointAsync` + `DividendUpdated` audit code) and api.ts update helpers (debtor/creditor/fixed-asset/inventory/dividend). Test `UpdateDividend_PersistsChangesAndLogsAudit`. Full suite **482 pass / 2 skip** (exit 0). Commit `6ecbaef`. (UI inline-edit wiring shares the BL-05 UI deferral above.)
- **BL-02 / BL-03 / BL-18** ✅ — `DocumentGeneratorService.GenerateAccountsPackageAsync` now renders a **Cash Flow Statement** and **Statement of Changes in Equity** for Medium/Full (from existing `FinancialStatementsService` outputs), and an **Independent Auditor's Report** when `AuditExempt == false` (suppressed when true). The auditor's report is a clearly-marked TEMPLATE for a Registered Auditor to complete — no fabricated opinion (Human decision #2). `GetIncludedPrimaryStatements` exposes the rendered section set; `FilingRegimeService.GetRequiredStatements` made public for cross-checking. Tests: required-statement coverage (Medium+Full), auditor's report toggles with exemption, small omits the extras, and the Medium PDF actually generates past the readiness gate and is larger than the small PDF (BL-18). Full suite **479 pass / 2 skip** (exit 0). Commit `afb4537`.
  - FLAG (Human decision #2): audited companies get a placeholder auditor's report; a human Registered Auditor must complete/sign it. NOTE: "appears in the PDF" is verified via the rendered-section list + a PDF-size delta (no PDF-text-extraction library is available; adding one risks the documented NuGet SSL issues).
- **BL-08 / BL-09** ✅ — `IxbrlService` now emits prior-year comparatives (new `prior`/`priorInstant` contexts + a comparative fact for every BS/P&L line) and entity/report metadata (`bus:EntityCurrentLegalOrRegisteredName`, period start/end), and is now well-formed XML (the raw `&nbsp;` is replaced with `&#160;`). Test `Ixbrl_EmitsPriorYearComparativesEntityMetadataAndWellFormedXml` parses the output via `XDocument`/`XmlReader` (DTD ignored), asserts comparatives + metadata, and checks tagged values equal the statement figures. Full suite **481 pass / 2 skip** (exit 0). Commit `2b15ae5`.
  - FLAG (Human decision #1): real ROS/CRO schema validation remains a human gate — v1 iXBRL ships as "support output, externally validated". Negative facts still use signed content rather than the `sign="-"` attribute (kept to avoid churn; not a conformance blocker for well-formedness).
- **BL-13** ✅ — `NotesDisclosureService.GenerateNotesAsync` now adds the Medium/Full-only notes (Turnover, Tax on Profit on Ordinary Activities, Financial Instruments, Capital Commitments), rendered even when nil. Test `Notes_MediumRegimeAddsFullerDisclosureSetBeyondSmall`. Full suite **480 pass / 2 skip** (exit 0). Commit `ca31277`.
- **BL-07** ✅ — `AdjustmentService` now generates the two missing section-E adjustments: (1) **loan-interest accrual** — interest on outstanding loans, stored as an Accrual creditor (so it's a balance-sheet liability) and expensed via the accruals path → stays balanced; routed to Bank Charges & Interest (6900); idempotent. (2) **director-loan reclassification** — overdrawn director's loan account presented as a receivable, P&L-neutral. Tests `Adjustments_AccrueLoanInterestAndKeepBalanceSheetBalanced`, `Adjustments_ReclassifyOverdrawnDirectorLoanAsReceivable`. Full suite **474 pass / 2 skip** (exit 0). Commit `7ada0cc`.
  - FLAG (Human decision #4, s.236 vs s.239): the reclassification's legal-basis text cites "s.236/s.239" without resolving the discrepancy — left for legal confirmation. Tests assert the coded behaviour only.
  - NOTE: loan interest is estimated on the closing balance × rate × period fraction and may double-count interest already paid via bank transactions — it is a *proposed* adjustment (has ApprovedAt workflow) for reviewer judgement, not auto-final. Director-loan reclassification is documented in the adjustments/trial balance but the entity-based balance sheet still does not present director loans as a line (full BS integration deferred — couples to the BL-01 architecture decision).
- **BL-06** ✅ — New `capital_allowance_claims` table (entity + EF migration `AddCapitalAllowanceClaims`) persists the actual per-asset wear-and-tear claim each period, written by `GenerateAutoAdjustmentsAsync`. `TaxComputationService` now reads prior claims from these records (was re-estimating from depreciation-entry period lengths, which under-counted once the accounting depreciation life ran out) and caps cumulative allowances at cost. Tests `CapitalAllowances_ClaimPersistedWhenAdjustmentsGenerated` and `CapitalAllowances_CapCumulativeClaimUsingPersistedPriorClaims`. `dotnet ef migrations has-pending-model-changes` clean. Full suite **472 pass / 2 skip** (exit 0). Commit `ad7bb57`.
